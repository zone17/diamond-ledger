// TODO(T013): Implement the agent/API adapter.
//             Exposes dl-core's four primitives over an HTTP or WASM boundary.
//             Same primitives, same contracts as the iOS app — parity by construction (Art. II).
//             See plan.md "adapters/agent".

fn main() {
    // TODO(T013): wire HTTP/WASM entrypoint and dispatch to dl_core primitives.
    println!("dl-agent: not yet implemented");
}
