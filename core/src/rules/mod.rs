// TODO(T005): Implement the deterministic rules engine:
//             - Game state machine (inning / half / outs / base occupancy)
//             - Runner advancement rules (force plays, tag plays, dropped-third-strike, etc.)
//             - Scoring (R, H, E, LOB accumulators — integer only)
//             All arithmetic must stay integer. See plan.md and data-model.md.
